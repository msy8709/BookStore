import React from 'react';
import styled from 'styled-components';

const $1 = () => {
    return (
        <$1Style>

        </$1Style>
    );
};

const $1Style = styled.div`
    
`;
export default $1;
